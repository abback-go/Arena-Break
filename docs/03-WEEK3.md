# 3주차 — 완성과 검증

> **이 문서를 보면서 순서대로 따라가세요.** 수업 3시간이 이 문서 하나로 진행됩니다.

**시작 전 조건**: 2주차 결과물(웨이브 3개가 순서대로 진행되고 풀링이 적용된 상태)이 동작해야 합니다.
진도가 밀렸다면 [막혔을 때](#막혔을-때)의 태그 점프를 먼저 하세요.

---

## 오늘 만드는 것

| 시간 | 내용 | 결과물 |
|---|---|---|
| 10분 | 시작 점검 | 2주차 결과물 동작 확인 |
| 40분 | **3-1** 게임 상태 머신 | `GameManager.cs` + 플레이어 체력 + 웨이브 5개 |
| 35분 | **3-2** HUD | `HUDController.cs` |
| 10분 | 쉬는 시간 | |
| 25분 | **3-3** 게임필 | 한 번에 하나씩 |
| 40분 | **3-4** AI 코드 리뷰 ★ | **오늘의 클라이맥스** |
| 20분 | **3-5** 빌드와 제출 | `.exe` + 제출물 4종 |

## 오늘 배우는 것

1주차는 **컨텍스트 설계**, 2주차는 **작업 분할과 검증**이었습니다. 마지막 주제는 이것입니다.

> **AI 산출물의 검증** — AI가 쓴 코드뿐 아니라, **AI가 쓴 리뷰도 검증 대상이다**

3-4에서 지금까지 쌓인 스크립트 전체를 AI에게 리뷰시킵니다.
그리고 **그 리뷰에서 틀린 지적을 찾아냅니다.** 이것이 오늘의 목표입니다.

---

# 0. 시작 점검 (10분)

## 0-1. 순서를 지켜 실행

```
1. Unity로 프로젝트 열기
2. Window → MCP for Unity → Toggle MCP Window → [Start Server]
3. 그다음에 터미널에서 claude 실행
```

## 0-2. 2주차 결과물 확인

`Play` 를 눌러 직접 확인합니다.

```
□ 웨이브 1이 시작되고 적 3마리가 스폰된다
□ 적이 나를 추적하고 가까이서 공격 로그를 남긴다
□ 적을 쏘면 죽고, 잠시 뒤 사라진다
□ 다 죽이면 3초 뒤 웨이브 2가 시작된다
□ 콘솔에 빨간 에러가 0개다
```

> Console 상단의 `Collapse` 는 **꺼둔 채로** 진행하세요. 오늘 로그를 자주 봅니다.

## 0-3. 지금 없는 것

오늘 채울 구멍을 먼저 확인합니다. 2주차 코드에 이런 주석이 남아 있습니다.

```csharp
// 3주차에 HUD로 옮긴다
Debug.Log($"발사 — 남은 탄약 {_currentAmmo}/{_magazineSize}");
```

```
□ 플레이어에게 Health 가 없다 — 적이 때려도 아무 일도 안 일어난다
□ 탄약과 체력이 콘솔에만 나온다 — 화면에 없다
□ 게임이 끝나지 않는다 — 죽어도, 다 깨도 그대로다
□ CLAUDE.md 의 목표는 5웨이브인데 웨이브가 3개뿐이다
```

오늘 이 넷을 닫습니다.

---

# 3-1. 게임 상태 머신 (40분)

## 먼저 — 플레이어에게 체력을 붙입니다

2주차에 만든 `Health` 를 그대로 씁니다. **새로 만들지 않습니다.**

```
씬의 Player 에 ArenaBreak.Core.Health 컴포넌트를 붙여줘.
최대 체력은 100.

끝나면 컴포넌트를 다시 읽어서 값이 들어갔는지 보여줘.
```

`Play` 를 눌러 적에게 맞아 보세요. `Player` 를 선택해두면 `Current Health` 가 줄어드는 것이 보입니다.

> **이것이 2주차 2-1에서 인터페이스를 나눈 이유입니다.**
> 플레이어용 체력 코드를 한 줄도 새로 쓰지 않았습니다.
> `WeaponSystem` 도 `EnemyAI` 도 고치지 않았습니다.

## 프롬프트 (그대로 붙여넣기)

```
게임 상태 머신을 만들어줘.

- 상태: Ready / Playing / GameOver / Cleared — enum + switch로 단순하게
- Ready에서 시작해 잠깐 뒤 Playing으로 (대기 시간은 인스펙터에서 조절)
- 플레이어 Health의 사망 event를 구독해서 GameOver로
- WaveSpawner의 전체 클리어 event를 구독해서 Cleared로
- GameOver / Cleared 에서: 커서 표시, Time.timeScale = 0
- 그 상태에서 R키를 누르면 현재 씬을 다시 로드
  (Time.timeScale을 1로 되돌리는 것을 잊지 말 것)
- 상태가 바뀌면 C# event로 알릴 것 — HUD가 구독할 것이다
- 구독한 event는 OnDisable에서 반드시 해제할 것
- GameOver / Cleared 로 갈 때 플레이어 조작을 멈출 것.
  timeScale = 0 은 Update를 멈추지 않아서, 안 끄면 죽은 뒤에도 총이 나간다
- Esc로 게임을 종료할 것 (Application.Quit). 어느 상태에서든 눌리게
- R키와 Esc는 InputSystem_Actions 에셋에 액션이 없다.
  WeaponSystem의 재장전처럼 코드에서 InputAction을 만들어 쓸 것

파일: Assets/Scripts/Core/GameManager.cs 하나만 생성.
WaveSpawner와 PlayerController는 수정하지 마.
씬은 건드리지 말고 세팅 절차만 알려줘.
```

### 여기서 지정한 것들

| 지정한 줄 | 없으면 생기는 일 |
|---|---|
| "R키는 액션이 없다" | 1주차 재장전 때와 같은 함정입니다. **한 번 겪은 함정은 프롬프트에 미리 씁니다** |
| "timeScale을 1로 되돌릴 것" | 재시작했는데 화면이 멈춰 있습니다. 가장 흔한 버그입니다 |
| **"플레이어 조작을 멈출 것"** | 이게 없으면 **죽은 뒤에 총알이 한 발 더 나갑니다.** 아래에 이유를 적었습니다 |
| **"Esc로 종료"** | 빌드는 전체 화면이라 닫기 버튼이 없습니다. 3-5에서 확인합니다 |
| "상태가 바뀌면 event로 알릴 것" | HUD가 `Update()`에서 상태를 폴링하게 됩니다 |
| "WaveSpawner는 수정하지 마" | 2주차에 완성한 것을 건드리지 않게 막습니다 |

### timeScale = 0 의 함정 ★★

`Time.timeScale = 0` 이 멈추는 것과 멈추지 않는 것을 구분해야 합니다.

| 멈춘다 | 멈추지 않는다 |
|---|---|
| `Time.deltaTime`, `Time.time` | **`Update()` 호출 자체** |
| `WaitForSeconds` | `WaitForSecondsRealtime`, `Time.unscaledTime` |
| 물리 시뮬레이션 | **입력(Input System)** |

여기서 두 가지가 나옵니다.

**1. 대기를 코루틴 + `WaitForSeconds` 로 만들면 영원히 안 돌아옵니다.**
정지 상태에서 시간이 흐르지 않기 때문입니다. `Time.unscaledTime` 이나 `WaitForSecondsRealtime` 을 써야 합니다.

**2. 정지시켰다고 플레이어가 멈추지 않습니다.** ★

`Update()` 는 계속 돌고 입력도 살아 있습니다. 실제로 이런 일이 생깁니다.

```
죽는다 → timeScale = 0 → 화면은 멈춤 → 좌클릭하면 총알이 한 발 나감
```

`Time.time` 이 얼어붙은 순간 `Time.time >= 다음발사시각` 이 한 번 참이 되기 때문입니다.
그래서 **정지시킬 때 플레이어 조작 컴포넌트를 꺼야 합니다.**

> 끄면 각 컴포넌트의 `OnDisable` 이 입력 액션까지 함께 해제해 줍니다.
> **"멈춘 것처럼 보인다"와 "멈췄다"는 다릅니다.**

## 코드 읽기

```
방금 만든 GameManager.cs에서 재시작 처리 부분을 설명해줘.
Time.timeScale을 언제 0으로 만들고 언제 1로 되돌리는지,
그 순서가 왜 그래야 하는지 중심으로.
```

## 씬 세팅

```
GameManager 컴포넌트를 씬의 GameManager 오브젝트에 붙여줘.
(2주차에 WaveSpawner를 붙인 그 오브젝트다)

- 플레이어 Health 참조 칸에 씬의 Player 를 연결
- WaveSpawner 참조 칸에 같은 오브젝트의 WaveSpawner 를 연결

끝나면 컴포넌트를 다시 읽어서 두 칸에 실제로 값이 들어갔는지 보여줘.
```

## 웨이브 2개 추가 — 코드 0줄 (5분)

`CLAUDE.md` 의 목표는 **5웨이브 생존**입니다. 지금은 3개뿐입니다.

```
Assets/Data 에 WaveData 에셋을 2개 더 만들어줘.

- Wave_4 : 적 12마리, 스폰 간격 0.5초
- Wave_5 : 적 16마리, 스폰 간격 0.4초
- 둘 다 적 프리팹은 Assets/Prefabs/Enemy.prefab

만든 뒤 GameManager 오브젝트의 WaveSpawner 웨이브 리스트 끝에 순서대로 추가해줘.
끝나면 리스트를 다시 읽어서 5개가 들어갔는지 보여줘.
```

> **스크립트를 한 줄도 안 고쳤습니다.**
> 2주차에 웨이브를 `ScriptableObject` 로 만든 이유가 이것입니다.
> 값을 바꾸는 데 컴파일이 필요 없고, 기획자가 직접 만질 수 있습니다.

## 검증

```
□ Play 하면 잠깐 뒤 웨이브 1이 시작된다
□ 적에게 계속 맞으면 죽고, 커서가 나타나며 화면이 멈춘다
□ 죽은 뒤 좌클릭해도 총알이 나가지 않는다
□ 죽은 뒤 마우스를 움직여도 시점이 돌지 않는다
□ 그 상태에서 R을 누르면 게임이 처음부터 다시 시작된다
□ 재시작 후 화면이 멈춰 있지 않다 (timeScale 복구 확인)
□ 재시작 후 커서가 다시 잠긴다
□ 웨이브가 5개까지 진행된다 (마지막 웨이브 번호를 로그로 확인)
□ 콘솔에 에러가 없다
```

> 5웨이브를 실제로 다 깨서 확인하기 어렵습니다.
> `Enemy` 프리팹의 `Max Health` 를 1로 잠깐 낮추면 빠르게 확인할 수 있습니다.
> **인스펙터 값이라 코드를 안 고쳐도 됩니다.** 확인 후 30으로 되돌리세요.

## 커밋

```bash
git add -A
git commit -m "feat: 게임 상태 머신과 재시작 추가"
```

---

# 3-2. HUD (35분)

## 먼저 알아야 할 것 — 지금 값이 어디 있나

| 보여줄 것 | 지금 어디에 있나 | 알림 방법이 있나 |
|---|---|---|
| 체력 | `Player` 의 `Health` | ✅ `HealthChanged` event |
| 웨이브 번호 | `WaveSpawner` | ✅ `WaveStarted` event |
| 게임 상태 | `GameManager` | ✅ 3-1에서 만든 event |
| **탄약** | `WeaponSystem` | ❌ **`Debug.Log` 뿐이다** |
| **킬 수** | 아무도 안 센다 | ❌ **없다** |

아래 둘은 **없는 것을 먼저 만들어야** 합니다. HUD부터 만들면 붙일 곳이 없습니다.

## 없는 것 두 개 먼저 (프롬프트)

```
HUD가 구독할 수 있도록 두 곳에 event를 추가해줘.

- WeaponSystem: 탄약이 변할 때 (현재, 최대) 를 알리는 event.
  기존 Debug.Log는 지울 것 — 이제 화면에 나온다
- WaveSpawner: 적이 죽을 때마다 누적 킬 수를 알리는 event.
  이미 각 적의 사망 event를 구독하고 있으니 거기서 세면 된다

기존 기능과 공개 event 시그니처는 그대로 유지할 것.
파일 2개만 수정하고 다른 파일은 건드리지 마.
```

수정 후 **직접 확인합니다.**

```bash
git diff Assets/Scripts/Player/WeaponSystem.cs Assets/Scripts/Core/WaveSpawner.cs
```

```
□ 기존 event 3개(WaveStarted / WaveCleared / AllWavesCleared)의 이름과 타입이 그대로인가?
□ 탄약·재장전·풀링 로직이 그대로인가?
□ 요청하지 않은 파일이 바뀌지 않았나?
```

## HUD 만들기 (프롬프트)

```
HUD를 만들어줘. UI Toolkit 말고 uGUI로.

표시할 것:
- 좌하단: 체력바. 어두운 배경 Image 안에 빨간 Image를 넣고,
  그 빨간 Image의 RectTransform 앵커 폭으로 줄일 것.
  Image.fillAmount는 쓰지 마 — 이유는 아래에 적었다.
  Health의 HealthChanged event 구독
- 우하단: 탄약 "현재 / 최대". WeaponSystem의 탄약 event 구독
- 상단 중앙: 웨이브 번호. WaveSpawner의 WaveStarted event 구독
- 상단 우측: 킬 수. WaveSpawner의 킬 event 구독
- 화면 중앙: GameOver / Cleared 안내 문구와 "R키로 재시작".
  평소에는 숨기고 GameManager의 상태 event로 켤 것

- Update()에서 폴링하지 말 것. 전부 event 구독으로
- 구독은 OnEnable, 해제는 OnDisable
- 참조는 전부 [SerializeField] 로 받을 것. Find 계열 금지

파일: Assets/Scripts/UI/HUDController.cs 하나만 생성.
UI 오브젝트는 아직 만들지 말고, 어떤 오브젝트가 필요한지 목록만 알려줘.
```

### "UI Toolkit 말고 uGUI로" 를 쓰는 이유

둘 다 Unity의 정식 UI 시스템입니다. 그냥 두면 AI가 하나를 골라줍니다.
**기술 선택은 사람이 합니다.** 이 수업은 인스펙터에서 눈으로 확인하는 흐름이라 uGUI가 맞습니다.

> MCP의 `manage_ui` 도구는 **UI Toolkit 전용**입니다. uGUI 전용 도구는 없어서
> Canvas와 위젯을 오브젝트·컴포넌트 단위로 하나씩 만들게 됩니다. 그 과정의 함정은 아래에 정리했습니다.

## UI 오브젝트 배치

AI가 알려준 목록대로 MCP에 시킵니다.

```
방금 알려준 UI 오브젝트들을 씬에 만들고 HUDController 에 연결해줘. MCP 도구로 직접 해줘.

끝나면 HUDController 의 각 참조 칸에 실제로 값이 들어갔는지 다시 읽어서 보여줘.
```

### 여기서 막히면 — MCP의 한계 ★

2주차 2-4에서 배운 것을 다시 씁니다.

| 넣는 것 | MCP로 되나 |
|---|---|
| 에셋 참조 (프리팹, ScriptableObject, 머티리얼) | ✅ |
| 씬 오브젝트 참조 — **단일** 칸 | ✅ |
| 씬 오브젝트 참조 — **배열·리스트** | ❌ 성공이라고 답하지만 비어 있음 |

HUD 참조는 전부 단일 칸이라 들어갑니다. 하지만 **`None` 인 칸이 있는지 눈으로 확인하세요.**

UI 오브젝트 생성 자체가 잘 안 되면 **손으로 만들어도 됩니다.** 오늘의 목표는 UI 배치 기술이 아닙니다.

```
GameObject > UI > Canvas
GameObject > UI > Legacy > Text
```

### uGUI를 코드·MCP로 만들 때 밟는 함정 5개 ★★

이 절은 **실제로 다섯 번 다 밟고 적은 것**입니다. HUD가 안 보이면 여기부터 보세요.

| # | 증상 | 원인 | 대응 |
|---|---|---|---|
| 1 | HUD가 **아예 안 보임** | 컴포넌트로 추가한 `Canvas` 는 **World Space가 기본**이다. 메뉴로 만들면 Overlay | `Render Mode` 를 `Screen Space - Overlay` 로 |
| 2 | 체력바가 **줄지 않고 항상 꽉 참** | `Image.fillAmount` 는 **Source Image(스프라이트)가 있어야** 동작한다. 코드로 만든 Image에는 없다 | 스프라이트를 넣거나, `RectTransform` 앵커 폭으로 줄인다 |
| 3 | HUD가 **가장자리만 잘려 안 보임** | Game 뷰 해상도가 창보다 크고 `Scale` 이 높으면 중앙만 보인다. HUD는 전부 가장자리에 있다 | 해상도를 `Free Aspect` 로, 또는 `Scale` 을 왼쪽 끝으로 |
| 4 | 값 수정이 거부됨 | `This cannot be used during play mode` | **Play를 먼저 멈춘다** |
| 5 | AI가 `Text` 컴포넌트를 읽으면 응답이 수천 줄 | `Text` 는 내부 텍스트 제너레이터의 정점 데이터까지 전부 뱉는다 | `Text` 는 **AI에게 읽히지 말고 인스펙터로 확인**한다 |

1번과 2번은 **에러가 하나도 안 납니다.** 콘솔은 깨끗한데 화면만 비어 있습니다.

> **이것이 3주차의 주제입니다.**
> 에러가 없다는 것은 코드가 맞다는 뜻이 아닙니다. **눈으로 본 것만 검증된 것입니다.**

### 폰트 — TextMeshPro를 쓰지 않는 이유

이 프로젝트는 **레거시 `UnityEngine.UI.Text`** 를 씁니다.
TextMeshPro를 처음 쓰면 "TMP Essentials를 임포트하겠느냐"는 창이 뜨고,
**그 창이 떠 있는 동안 MCP가 멈춥니다.** 레거시 `Text` 는 기본 폰트가 자동으로 잡혀 그냥 나옵니다.

## 검증

> **먼저 Game 뷰 상단의 해상도를 `Free Aspect` 로 두세요.**
> `Full HD (1920×1080)` 처럼 고정 해상도가 창보다 크면 가장자리가 잘려
> **HUD가 하나도 없는 것처럼 보입니다.**

```
□ 체력바가 있고, 적에게 맞으면 줄어든다
□ 탄약이 화면에 나오고, 쏘면 줄어든다
□ R로 재장전하면 탄약 표시가 최대로 돌아온다
□ 웨이브 번호가 웨이브마다 바뀐다
□ 킬 수가 적을 죽일 때마다 올라간다
□ 죽으면 화면 중앙에 안내 문구가 나온다
□ 콘솔에 탄약 로그가 더 이상 안 나온다 (화면으로 옮겼으므로)
□ 콘솔에 에러가 없다
```

## 커밋

```bash
git add -A
git commit -m "feat: HUD 추가, 탄약/킬 이벤트 노출"
```

---

# 쉬는 시간 (10분)

---

# 3-3. 게임필 — 한 번에 하나씩 (25분)

## 프롬프트 (그대로 붙여넣기)

```
타격감을 넣어줘. 단, 한 번에 하나씩 하고 매번 내가 테스트할게.

1) 적 피격 시 머티리얼을 0.05초 흰색으로 플래시
2) 피격 시 카메라 셰이크 (0.1초, 강도는 인스펙터에서 조절)
3) 명중 지점에 파티클

1번부터 시작해. 2번은 내가 말하기 전에는 하지 마.
```

## 이 프롬프트의 핵심

| 줄 | 역할 |
|---|---|
| "한 번에 하나씩" | 할 일 전체를 보여주되 **실행 범위를 1개로 제한** |
| **"매번 내가 테스트할게"** | 검증 주체를 명시합니다 |
| **"2번은 내가 말하기 전에는 하지 마"** | 이 줄이 없으면 3개를 한 번에 만듭니다 |

## 관찰할 것 ★

```
□ AI가 1번만 만들었나, 아니면 3개를 다 만들었나?
```

3개를 다 만들었다면 **되돌리고 프롬프트를 고쳐서 다시 하세요.**

```bash
git checkout .
```

> 이것도 오늘의 학습 내용입니다. **"하지 마"를 어떻게 써야 먹히는가.**
> 1주차 1-3의 비교 실험과 같은 원리입니다.

## 진행 방법

1번을 만들고 → 테스트하고 → 커밋하고 → 2번을 요청합니다.

```
1번 확인했어. 2번 카메라 셰이크로 넘어가줘.
```

시간이 부족하면 **1번만 하고 넘어가도 됩니다.** 3-4가 더 중요합니다.

## 커밋

```bash
git add -A
git commit -m "feat: 적 피격 플래시 추가"
```

---

# 3-4. AI 코드 리뷰 (40분) ★

> **이 수업 3주의 클라이맥스입니다.** 오늘 가장 중요한 40분입니다.

## 왜 하는가

지금까지 AI가 쓴 코드를 여러분이 검증했습니다.
이번에는 **AI에게 검증을 시키고, 그 검증 결과를 여러분이 다시 검증합니다.**

## 프롬프트 (그대로 붙여넣기)

```
지금까지 작성된 Assets/Scripts 전체를 리뷰해줘.

다음 관점으로 문제를 찾아 심각도 순으로 정리:
- Update()에서의 불필요한 연산 / GetComponent 호출
- null 참조 가능 지점
- event 구독 후 해제 누락 (메모리 누수)
- 책임이 뒤섞인 클래스
- CLAUDE.md 규약 위반

각 항목마다 파일:줄 번호와 수정 방향을 제시하되, 아직 고치지는 마.
```

마지막 줄이 중요합니다. **고치기 전에 판단할 시간을 확보합니다.**

## 진행 방법 — 순서를 지키세요

### 1단계. 지적을 하나씩 열어 확인한다 (15분)

리뷰 결과를 그대로 믿지 마세요. **파일을 직접 열어 그 줄을 봅니다.**

각 지적에 대해 셋 중 하나로 판정합니다.

| 판정 | 뜻 |
|---|---|
| **맞다** | 실제로 그 줄에 그 문제가 있다 |
| **틀렸다** | 그런 코드가 없거나, 문제가 아니다 |
| **애매하다** | 문제이긴 하나 이 프로젝트 규모에선 과하다 |

### 2단계. 틀린 지적을 최소 1개 찾는다 (10분)

**반드시 섞여 나옵니다.** 자주 나오는 틀린 지적:

| 흔한 잘못된 지적 | 왜 틀렸나 |
|---|---|
| "`Update()`에서 `GetComponent` 호출" | `Awake()` 에서 캐싱해 뒀는데 못 본 경우 |
| "event 해제 누락" | `OnDisable` 에 있는데 놓친 경우, 또는 지역 함수로 스스로 해제하는 패턴을 이해 못 한 경우 |
| "존재하지 않는 줄 번호" | 줄 번호가 실제 파일과 어긋난 경우 |
| "null 체크 필요" | `[RequireComponent]` 로 보장되는 참조인 경우 |

찾았으면 **근거를 들어 반박해 보세요.**

```
<지적 내용>은 틀린 것 같아. <파일:줄>을 보면 <실제 코드>야.
내가 잘못 이해한 건지, 지적이 틀린 건지 확인해줘.
```

> **AI가 순순히 물러서는지 보세요.**
> 근거 없이 "맞습니다, 죄송합니다"라고 하면 그것도 신뢰할 수 없다는 뜻입니다.

### 3단계. 우선순위 3개만 고친다 (10분)

전부 고치지 마세요. **가장 심각한 3개만** 고릅니다.

```
확인해보니 <항목 A>, <항목 B>, <항목 C> 는 실제 문제가 맞아.
이 3개만 고쳐줘. 나머지는 두고.
각 수정마다 무엇을 왜 바꿨는지 한 줄로 설명해줘.
```

### 4단계. 다시 플레이 테스트한다 (5분)

```
□ 웨이브가 정상 진행된다
□ HUD가 정상 갱신된다
□ 죽고 재시작이 된다
□ 콘솔에 에러가 없다
```

**리뷰 수정이 멀쩡한 코드를 망가뜨리는 일이 실제로 있습니다.**

## 기록 — 이것이 회고의 핵심 근거입니다

`docs/my-prompts.md` 의 **3-4** 칸에 아래 표를 채우세요.

```markdown
| # | AI의 지적 | 내 판정 | 근거 |
|---|---|---|---|
| 1 | (지적 내용) | 맞다 | (확인한 파일:줄) |
| 2 | (지적 내용) | **틀렸다** | (왜 틀렸는지) |
```

> **틀린 지적을 하나도 못 찾았다면, 검증을 안 한 것입니다.**
> 다시 보세요.

## 커밋

```bash
git add -A
git commit -m "refactor: 코드 리뷰 지적 3건 반영"
```

---

# 3-5. 빌드와 제출 (20분)

## 빌드 전 점검

```
Windows Standalone 빌드 설정을 점검해줘.
(Unity 6.3은 File > Build Settings가 아니라 File > Build Profiles다)

포함 씬, 회사명/제품명, 해상도 설정, 커서 잠금 동작을 확인하고
빌드 후 흔히 나는 문제(에디터에서만 되는 코드)가 있는지 스크립트를 훑어봐.
```

### 에디터에서만 되는 코드란

| 흔한 것 | 빌드에서 생기는 일 |
|---|---|
| `Debug.Log` 가 많음 | 성능 저하. 치명적이진 않음 |
| `UnityEditor` 네임스페이스 사용 | **빌드 자체가 실패합니다** |
| `Application.isEditor` 분기 | 빌드에서 다른 경로를 탑니다 |
| 종료 수단이 없음 | **창을 못 닫습니다.** 아래를 꼭 읽으세요 |

### 빌드는 전체 화면입니다 ★

기본 빌드는 전체 화면이라 **닫기 버튼이 없습니다.** 게임 안에 종료 수단이 없으면
`Alt+F4` 나 작업 관리자로만 빠져나올 수 있습니다.

에디터의 `Esc` 는 커서를 풀어주는 **에디터 전용 동작**이고, 빌드에는 그 동작이 없습니다.
그래서 3-1에서 `Esc` → `Application.Quit()` 을 넣었습니다.

> **`Application.Quit()` 은 에디터에서 아무 일도 하지 않습니다.**
> 에디터에서 아무리 눌러 봐도 검증되지 않습니다. **빌드로만 확인할 수 있는 항목입니다.**
>
> 오늘 배운 것이 여기서도 반복됩니다 — 에러가 없다는 것이 정상이라는 뜻은 아닙니다.

## 빌드

```
File > Build Profiles → Windows 프로필 선택 → Build
```

출력 폴더는 **프로젝트 밖**에 만드세요. 안에 만들면 `.gitignore` 에 안 걸려 리포에 들어갑니다.

## 빌드 검증 — 에디터가 아니라 exe로

```
□ exe가 실행된다
□ 이동 / 사격 / 재장전이 된다
□ HUD가 보인다
□ 웨이브가 진행된다
□ 죽으면 게임오버 화면이 나온다
□ R로 재시작된다
□ Esc를 누르면 창이 닫힌다   ← 에디터에서는 확인 불가능한 항목
```

빌드는 프로젝트 폴더 밖에 만들었으므로 리포에 들어가지 않습니다.
첫 빌드는 1~2분, 두 번째부터는 10초 안팎입니다.

## 제출물 4종

| 항목 | 형식 |
|---|---|
| 게임 빌드 | Windows `.exe` 를 zip으로 |
| 소스 코드 | 리포지토리 URL |
| **프롬프트 로그** | `docs/my-prompts.md` |
| **회고** | `docs/retrospective.md` |

`docs/retrospective.md` 는 **오늘 씁니다.** 시간이 지나면 실패한 순간이 기억나지 않습니다.
`my-prompts.md` 의 실패 기록을 열어 놓고 쓰세요.

## 마지막 커밋

```bash
git add -A
git commit -m "docs: 프롬프트 로그와 회고 작성"
git push origin main
```

---

# 오늘 끝내야 하는 것

```
□ Player 에 Health 부착 — 적에게 맞으면 체력이 줄어드는 것 확인
□ GameManager.cs — 게임오버 / 재시작 / 죽은 뒤 조작 정지 확인 + 커밋
□ WaveData 에셋 5개 (코드 0줄로 2개 추가)
□ HUDController.cs — 체력·탄약·웨이브·킬 표시 확인 + 커밋
□ 게임필 최소 1개 + 커밋
□ AI 코드 리뷰 실행 + 틀린 지적 최소 1개 발견 + 기록
□ 우선순위 3개 수정 후 재테스트 + 커밋
□ Windows 빌드 성공 + exe로 동작 확인
□ my-prompts.md 3-1 ~ 3-5 칸 채우기
□ retrospective.md 작성
□ git push
```

---

# 막혔을 때

## 진도가 밀렸다면

```bash
git add -A && git commit -m "wip"
git fetch upstream --tags
git checkout -b rescue w3-step1
```

돌아오려면 `git checkout main`.

| 태그 | 상태 |
|---|---|
| `w2-complete` | 2주차 완료 (웨이브, 풀링) |
| `w3-step1` | + 상태 머신, HUD |
| `w3-complete` | + 게임필, 빌드 설정 |

## 3회 규칙

같은 문제로 **3번 요청했는데도 안 되면 되돌리세요.**

```bash
git checkout .
```

## 그 밖의 문제

[TROUBLESHOOTING.md](TROUBLESHOOTING.md)

---

# 3주가 끝나고

여러분이 3주 동안 배운 것은 Unity가 아닙니다.

| 주차 | 배운 것 |
|---|---|
| 1주차 | **컨텍스트 설계** — 무엇을 알려주고 무엇을 막을 것인가 |
| 2주차 | **작업 분할과 검증** — 큰 것을 어떻게 쪼개고 조각마다 어떻게 확인하는가 |
| 3주차 | **AI 산출물의 검증** — AI의 리뷰도 틀린다 |

이 셋은 게임에만 쓰이지 않습니다.

> **명세를 쓰는 능력 = AI를 쓰는 능력**
>
> 그리고 그 결과를 **검증할 줄 아는 것**이 명세를 쓰는 능력보다 먼저입니다.
